#include <stdio.h>

int main() {
    int x, y, z;
    x=10, y=100, z=1000;
    float pi=3.14;
    char Grade = 'A';
    //printf the values
    printf("Sum of x, y and z is: %d\n", x+y+z);
    printf("Then multiple by pi, we have: %f\n", (x+y+z)*pi);
    printf("My Grade is: %c,\n", Grade);
    return 0;
}
